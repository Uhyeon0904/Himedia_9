import {handleActions} from 'redux-actions'

/* 메뉴 모듈 */
/* 초기값 */
const initialState = {};

/* 액션 */

/* 리듀서 함수 
* handleActions: */
const menuReducer = handleActions({

}, initialState);
export default menuReducer;